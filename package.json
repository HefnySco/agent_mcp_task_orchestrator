{
  "name": "sequential-mcp",
  "version": "1.2.0",
  "description": "MCP server for sequential task execution - manage and execute tasks in order with dependencies",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "test": "tsc --project tsconfig.test.json && node --test dist/test/sequentialService.test.js"
  },
  "keywords": [
    "mcp",
    "sequential",
    "task-execution",
    "workflow",
    "dependencies"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.22.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.0",
    "typescript": "^5.0.0"
  }
}
